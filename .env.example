# Robin Environment Configuration

# ========================================
# REQUIRED: Claude API Authentication
# ========================================
# Option 1: Use Anthropic API Key (recommended for servers)
ANTHROPIC_API_KEY=your-api-key-here

# Option 2: Use Claude Code OAuth token (from `claude setup-token`)
# CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token

# ========================================
# Database
# ========================================
POSTGRES_USER=robin
POSTGRES_PASSWORD=robin_dev_password
POSTGRES_DB=robin

# ========================================
# Optional Settings
# ========================================
DEFAULT_MODEL=claude-sonnet-4-20250514
# CORS_ORIGINS=["http://localhost:3000"]
